# name: Terraform Infrastructure

# on:
#   push:
#     branches: [main]
#     paths: ["terraform/**"]
#   pull_request:
#     paths: ["terraform/**"]
#   workflow_dispatch:
#     inputs:
#       action:
#         description: "Terraform action to run"
#         type: choice
#         options: [plan, apply, destroy]
#         default: plan

# permissions:
#   contents: read
#   pull-requests: write

# env:
#   TF_DIR: terraform
#   ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
#   ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
#   ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
#   ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
#   # Non-secret TF vars
#   TF_VAR_b2c_tenant_name: ${{ vars.B2C_TENANT_NAME }}
#   TF_VAR_b2c_tenant_id: ${{ vars.B2C_TENANT_ID }}
#   TF_VAR_b2c_client_id: ${{ vars.B2C_CLIENT_ID }}
#   # Secret TF vars
#   TF_VAR_db_admin_password: ${{ secrets.DB_ADMIN_PASSWORD }}

# jobs:
#   terraform:
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: ${{ env.TF_DIR }}
#     steps:
#       - uses: actions/checkout@v4

#       - uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: "1.8.5"

#       - name: Terraform Init (remote state in Azure Storage)
#         run: |
#           terraform init \
#             -backend-config="resource_group_name=${{ vars.TFSTATE_RG }}" \
#             -backend-config="storage_account_name=${{ vars.TFSTATE_STORAGE }}" \
#             -backend-config="container_name=tfstate" \
#             -backend-config="key=vms.terraform.tfstate"

#       - name: Terraform Format & Validate
#         run: |
#           terraform fmt -check -recursive
#           terraform validate

#       - name: Terraform Plan
#         id: plan
#         run: terraform plan -no-color -out=tfplan

#       - name: Comment plan on PR
#         if: github.event_name == 'pull_request'
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const output = `#### Terraform Plan 📋\n\`\`\`\n${{ steps.plan.outputs.stdout }}\n\`\`\``;
#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: output.substring(0, 65000)
#             });

#       # Apply: automatic on push to main, or manual dispatch with action=apply
#       - name: Terraform Apply
#         if: >
#           (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
#           (github.event_name == 'workflow_dispatch' && inputs.action == 'apply')
#         run: terraform apply -auto-approve tfplan

#       - name: Terraform Destroy (manual only)
#         if: github.event_name == 'workflow_dispatch' && inputs.action == 'destroy'
#         run: terraform destroy -auto-approve
